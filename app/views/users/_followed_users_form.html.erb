<% @user.followed_users.each do |followed| %>
    <div class="container-child col-md-2 col-sm-4 col-xs-6 ">
      <%= profile_img(followed) %></br>
      <strong><%= followed.name %></strong></br>
    </div>
<% end %>
