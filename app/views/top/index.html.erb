<% flash.each do |key, value| %>
  <% key = "info" if key == "notice" %>
  <%= content_tag(:div, value, class: "alert alert-#{key}") %>
<% end %>

<section class="main-header">
  <!-- <p class="flash"><%= notice %><%= alert %></p> -->
  <h1>kitamubook</h1>
　<img src="<%= asset_path "group_people_smartphone.png" %>">
</section>
